name: CI

on:
  push:
  pull_request:

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: Install
        run: pip install -e . pytest ruff mypy
      - name: pytest
        run: pytest tests/ -x -q
      - name: ruff
        run: ruff check launch_gate/
      - name: mypy
        run: mypy launch_gate/ --ignore-missing-imports
